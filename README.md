# 🚀 Portfolio - Technical Specialist

Современное портфолио технического специалиста по автоматизации бизнес-процессов. Проект создан на React + Vite + TypeScript + Tailwind CSS.

## ✨ Особенности

- 🎨 Современный dark-дизайн с градиентами и анимациями
- 📱 Полностью адаптивный дизайн (mobile-first)
- ⚡ Быстрая загрузка благодаря Vite
- 🎯 TypeScript для типобезопасности
- 🎭 Плавные анимации и переходы
- 🔍 SEO-оптимизация
- 📦 Компонентная архитектура

## 🛠 Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev-сервер
- **Tailwind CSS** - утилитарные стили
- **Font Awesome** - иконки
- **Space Grotesk & JetBrains Mono** - шрифты

## 📋 Структура проекта

```
src/
├── api/              # API функции
│   ├── certificates.ts
│   └── contact.ts
├── components/       # Переиспользуемые компоненты
│   └── Header.tsx
├── data/            # Данные
│   └── cases/       # Кейсы и проекты
│       ├── case1-webinar-flexible.ts
│       ├── case2-installment-automation.ts
│       ├── case3-timer-automation.ts
│       ├── case4-contest-automation.ts
│       ├── case5-marathon-automation.ts
│       ├── remaining-cases.ts
│       └── index.ts
├── pages/           # Страницы
│   └── MainPage.tsx
├── sections/        # Секции страниц
│   └── MainPage/
│       ├── About.tsx
│       ├── Cases.tsx
│       ├── Contact.tsx
│       ├── Hero.tsx
│       └── Technologies.tsx
├── App.tsx          # Главный компонент
├── index.tsx        # Точка входа
└── website.css      # Глобальные стили
```

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск dev-сервера

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

### Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Предпросмотр production-сборки

```bash
npm run preview
```

## 📝 Настройка контента

### Добавление нового кейса

1. Создайте файл в `src/data/cases/` (например, `case11-new-project.ts`)
2. Экспортируйте объект с данными:

```typescript
export const case11NewProject = {
  id: 11,
  category: 'Автоматизация',
  categoryClass: 'bg-emerald-400/20 text-emerald-400',
  gradientClass: 'bg-gradient-to-br from-emerald-400/20 to-green-500/20',
  icon: 'fas fa-cogs',
  title: 'Название проекта',
  description: 'Краткое описание',
  fullDescription: 'Полное описание проекта',
  result: '100%',
  resultLabel: 'Автоматизация',
  duration: '2 недели',
  status: 'completed',
  filter: 'automation',
  mainTech: ['GetCourse', 'API'],
  allTech: ['GetCourse', 'API', 'JavaScript'],
  results: [
    'Результат 1',
    'Результат 2'
  ]
}
```

3. Добавьте импорт в `src/data/cases/index.ts`

### Изменение контактных данных

Отредактируйте компонент `src/sections/MainPage/Contact.tsx`:

```typescript
const contactMethods = [
  {
    icon: 'fas fa-envelope',
    title: 'Email',
    value: 'your@email.com',  // <-- Измените здесь
    link: 'mailto:your@email.com',
    description: 'Описание',
    color: 'var(--electric-blue)'
  },
  // ... остальные методы
]
```

### Изменение информации "О себе"

Отредактируйте компонент `src/sections/MainPage/About.tsx`

## 🎨 Кастомизация стилей

### Цветовая схема

Основные цвета определены в `src/website.css`:

```css
:root {
  --navy-950: #0a0d1a;      /* Основной фон */
  --navy-900: #0f1629;      /* Вторичный фон */
  --navy-800: #1e293b;      /* Границы */
  --electric-blue: #0ea5e9; /* Акцентный цвет */
  --cyan-400: #22d3ee;      /* Дополнительный акцент */
  --violet-400: #a78bfa;    /* Третий акцент */
  --emerald-400: #34d399;   /* Успех */
  --amber-400: #fbbf24;     /* Предупреждение */
}
```

### Tailwind конфигурация

Расширьте конфигурацию в `tailwind.config.js`

## 📱 Адаптивность

Проект полностью адаптивен для всех размеров экранов:

- 📱 Mobile: < 768px
- 💻 Tablet: 768px - 1024px
- 🖥 Desktop: > 1024px

## 🌐 Деплой

### Vercel

```bash
npm install -g vercel
vercel
```

### Netlify

```bash
npm run build
# Загрузите папку dist/ на Netlify
```

### GitHub Pages

1. Обновите `vite.config.ts`:

```typescript
export default defineConfig({
  plugins: [react()],
  base: '/your-repo-name/'
})
```

2. Запустите сборку и деплой:

```bash
npm run build
```

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Technical Specialist - Автоматизация бизнес-процессов

- Email: evgeniyagursieva@gmail.com
- Telegram: [@gurtsievae](https://t.me/gurtsievae)

---

Создано с ❤️ на React + Vite

